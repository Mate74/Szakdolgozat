import Entity from "./Entity";

export default class Warrior  extends Entity {
    
    constructor(scene,sprite,damage,hp,velocity,cost,team)
    {
        super(scene,sprite,damage,hp,velocity,cost,team)
    }
}